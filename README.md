# Student Dashboard

This is a small Vite + React demo app (branch: dashboard-v1) showing a student dashboard with Marks and Attendance pages. 

Run locally:

1. npm install
2. npm run dev

Files added: basic components, Tailwind setup, mock data in src/data/students.json
